package com.soprasteria.academy.tp4.skip;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.batch.core.step.skip.SkipPolicy;

import com.soprasteria.academy.tp4.exception.TP4FiltreException;

/**
 * A FAIRE
 * 1. Declarer cette classe comme etant une implementation d'une skip policy.
 * 2. Ajouter la methode souldSkip attendue
 * 3. Definir un attribut entier étant la limite maximum de skip autorisé.
 * 4. Dans son implementation, indiquer que le traitement ignore toutes les exceptions de type TP4FiltreException,
 * tant que la limite n'est pas dépassée.
 */
public class TP4SkipPolicy {

}